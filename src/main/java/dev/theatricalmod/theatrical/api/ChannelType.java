/*
 * LDEncore -- Mica Technologies' fork of Theatrical, ported to Forge 1.12.2.
 *
 * Part of the 1.12.2 port. Written by Mica Technologies after upstream's
 * api/ChannelType.java (Theatrical Team, Apache License 2.0); the upstream file targets
 * Forge 1.16 and this is its 1.12.2 counterpart, not a copy.
 *
 * CHANGED FROM UPSTREAM: none beyond the port (identical, no Minecraft API involved).
 */
package dev.theatricalmod.theatrical.api;

public enum ChannelType {
    INTENSITY,
    RED,
    GREEN,
    BLUE,
    PAN,
    TILT,
    FOCUS
}
